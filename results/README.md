# Benchmark results

Raw `.log` / `.json` from `91_bench.sh` and `92_sweep.sh` are gitignored (they
are large and machine-specific). The curated numbers live in the root
`README.md`; this directory is where a run lands locally after `bash sync.sh pull`.

Naming: `<mode>-<profile>[-<backend>]-isl<N>-osl<N>-c<concurrency>`.
